node_modules/
.env
dist/

# Claude Code local config (per-machine settings + permissions)
.claude/settings.json
.claude/settings.local.json

# Local test data fixtures (not part of the app, may contain made-up SSNs etc)
OpsFloa_TestData.txt

